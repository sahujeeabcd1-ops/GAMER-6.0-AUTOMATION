const c=document.getElementById('command'),r=document.getElementById('run'),o=document.getElementById('result');r.onclick=()=>{let x=c.value.trim();if(!x){o.textContent='पहले command लिखो।';return}let t=x.toLowerCase(),task=t.includes('gta')||t.includes('gamer')?'Task 1 — GAMER 6.0':t.includes('api')||t.includes('github')?'Task 3 — AI API Library':'Task 2 — Personal AI Studio';o.innerHTML='<b>Command received ✅</b><br>Selected: '+task+'<br><br>Workflow: Research → Script → AI Review → Media Generation → Final Approval.<br><br>यह frontend prototype है; असली AI/API execution backend phase में जुड़ेगा।'};